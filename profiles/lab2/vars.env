# Cluster identity
TALOS_CLUSTER_NAME=talos-lab-2

# Where provision stores generated configs and stamps
TALOS_DIR=/var/lib/talos-lab-2

# Libvirt network
TALOS_NET_NAME=talosnet2
TALOS_BRIDGE_NAME=virbr-talosnet2
TALOS_GATEWAY=192.168.124.1
TALOS_DHCP_START=192.168.124.100
TALOS_DHCP_END=192.168.124.254

# Where kubeconfig is written
KUBECONFIG_OUT=/root/.kube/talos-lab-2.config

# --- Add-ons ---
# Metrics Server enables: kubectl top nodes / kubectl top pods
# Defaults are set in scripts/common.sh; override here if you want.
METRICS_SERVER_ENABLE=1
METRICS_SERVER_KUBELET_INSECURE_TLS=1
METRICS_SERVER_PREFERRED_ADDRESS_TYPES=InternalIP,Hostname
